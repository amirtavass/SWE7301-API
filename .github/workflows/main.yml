name: Python CI/CD Pipeline

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: "3.12"

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        python -c "from app.db import Base; from run import engine; import app.models.observation; Base.metadata.create_all(engine)"

    - name: Initialize Database
      run: |
        export PYTHONPATH=$PYTHONPATH:$(pwd)/backend
        python -c "from run import engine; from app.models.observation import ObservationRecord; Base.metadata.create_all(engine)"
      
    - name: Run Tests
      run: |
        export PYTHONPATH=$PYTHONPATH:$(pwd)/backend
        pytest backend/tests/

